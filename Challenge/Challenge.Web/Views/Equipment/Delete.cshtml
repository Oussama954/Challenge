@model Challenge.Web.Models.EquipmentModel

@{
    ViewBag.Title = "Delete";
}

    <h2>@Resources.Equipment.Delete</h2>

    <h3>@Resources.Equipment.DeleteVerify</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SerialNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SerialNumber)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.NextControlDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NextControlDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Picture)
        </dt>

        <dd>
            @if (Model.PictureUrl != null)
            {
                <img src="@Html.DisplayFor(model => model.PictureUrl)" width="96" height="65">
            }
        </dd>
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value=@Resources.Equipment.Delete class="btn btn-default" /> |
            @Html.ActionLink(@Resources.Equipment.BacktoList, "Index")
        </div>
    }
</div>
